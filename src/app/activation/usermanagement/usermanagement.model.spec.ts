import { UsermanagementModel } from './usermanagement.model';

describe('UsermanagementModel', () => {
  it('should create an instance', () => {
    expect(new UsermanagementModel()).toBeTruthy();
  });
});
