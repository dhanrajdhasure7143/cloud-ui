import { DynamicCompHostDirective } from './dynamic-comp-host.directive';

describe('DynamicCompHostDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicCompHostDirective();
    expect(directive).toBeTruthy();
  });
});
